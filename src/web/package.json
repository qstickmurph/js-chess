{
  "name": "web",
  "version": "0.0.0",
  "main": "main.ts",
  "scripts": {
    "build": "tsc && cp -r main.html templates/ assets/ dist/",
    "test": "echo \"No test script specified. Skipping.\" && exit 0",
    "lint": "echo \"No lint script specified. Skipping.\" && exit 0",
    "tsc": "tsc"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
